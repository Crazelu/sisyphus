import 'package:flutter/material.dart';

class TradeModal extends StatelessWidget {
  const TradeModal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
